Class name: ChangeDirectory
Parent class (if any): ShellCommand
Subclasses (if any): 

Responsibilities:
* Report errors if user tries to change to forbidden directories
* Change the CurrentDirectory to the user-specified one 
* Differentiate between a full path or a path relative to current directory

Collaborators:
* CurrentDirectory